# Emulator params for project demo-warp12 (yarn test:functions:emulator).
# Not production secrets — safe to commit.
BOOTSTRAP_ADMIN_SECRET=e2e-bootstrap-admin-secret
CERTIFICATE_SIGNING_SECRET=e2e-certificate-signing-secret
