/** Hover text for the per-captain Lookahead checkbox in local and online lobby UIs. */
export const LOOKAHEAD_TOOLTIP =
  'Forward-search AI: simulates moves through the real rules engine a couple of turns ahead. Does not see opponent tiles—only hand counts; it guesses plausible holdings several times and picks the best average outcome. Slower but sharper than greedy heuristics. See Operations Manual §VII.';
